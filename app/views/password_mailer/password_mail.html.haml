%h1 Starter-bootstrap
%p A password reset was triggered at starter-bootstrap for your account.  Please click the below link to set a new password for your account.
%p If you did not request a password reset, just ignore this email.

%p
  - if ActionMailer::Base.default_url_options[:protocol].nil?
    = link_to "password reset", "http://" + ActionMailer::Base.default_url_options[:host] + new_password_path(:token => @token)
  - else
    = link_to "password reset", ActionMailer::Base.default_url_options[:protocol] + ActionMailer::Base.default_url_options[:host] + new_password_path(:token => @token)
