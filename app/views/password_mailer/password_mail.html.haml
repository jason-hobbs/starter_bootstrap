%h1 Starter-bootstrap
%p A password reset was triggered at starter-bootstrap for your account.  Please click the below link to set a new password for your account.
%p If you did not request a password reset, just ignore this email.

%p
  = link_to "password reset", ENV['HOST_PROTOCOL'] + ENV['HOST_DOMAIN'] + new_password_path(:token => @token)
